# TradeBreakClassifier
Claude API workflow that takes a trade break description (text input) and returns structured classification — type (settlement fail, price mismatch, quantity mismatch, corporate action issue, etc.), severity, suggested resolution path, and required next actions.
